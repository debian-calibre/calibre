# -*- mode: conf -*-

# Linked against Python library instead of libc
# All symbols are depends on Python library and uchardet
calibre-bin binary: library-not-linked-against-libc [usr/lib/calibre/calibre/plugins/uchardet.so]
