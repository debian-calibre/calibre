#!/bin/sh

set -eu

case "$1" in
    install|upgrade)
	# Unbreak MathJax path upgrade.
	# Replace directory symbolic link to normal file tree.
	if [ -h /usr/share/calibre/mathjax ] ; then
	    rm /usr/share/calibre/mathjax
	fi
	;;
    *)
	# Nothing to do
	;;
esac

#DEBHELPER#
