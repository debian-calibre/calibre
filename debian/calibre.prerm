#!/bin/sh

set -eu

if which pyclean >/dev/null 2>&1; then
  pyclean -p calibre 
else
  dpkg -L calibre | grep \.py$ | while read file
  do
    rm -f "${file}"[co] >/dev/null
  done
fi

#DEBHELPER#


